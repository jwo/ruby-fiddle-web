<% if @riddle.errors.any? %>
  $("#riddle_form").html("<%= escape_javascript(render partial: "form") %>");
<% else %>
  window.location.replace("<%= url_for(versioned_riddle_path(@riddle, @riddle.versions.count)) %>");
<% end %>
